<h2>Editing Book</h2>

<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this book from being saved:
<br>
<% @book.errors.full_messages.each do |message| %>
･<%= message %><br>
<% end %>
<% end %>


<%= form_for @book do |f| %>

<%= f.label :title %><br>
<%= f.text_field :title %><br>

<%= f.label :body %><br>
<%= f.text_area :body %><br>

<%= f.submit %>
<% end %>

<%= link_to "Show", book_path, class: "btn btn-default" %>|
<%= link_to "Back", books_path, class: "btn btn-default" %>